[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "immediate-eos-env"
version = "0.1.0"
dependencies = [
    "verifiers",
]

[project.entry-points."verifiers"]
immediate-eos-env = "immediate_eos_env:load_environment"
